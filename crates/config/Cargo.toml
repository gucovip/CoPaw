[package]
name = "copaw-config"
version = "0.1.0"
edition = "2021"

[dependencies]
dirs = "5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
tokio = { version = "1", features = ["fs", "rt", "time", "sync", "macros", "test-util"] }
notify = "6.0"
tracing = "0.1"
